# 贡献指南

欢迎提交缺陷报告、官方数据适配修复、规则核实和测试改进。请勿提交预测保证或将合成数据伪装为官方开奖。

## 本地检查

Python 3.10+，不需要安装运行依赖：

```sh
python -m unittest discover -s tests -v
python tools/smoke_test.py
```

macOS/Linux 可使用 python3。smoke_test 完全离线，在临时目录中使用明确标注的合成数据，结束后清理，不读取或修改日常数据库。CI 对 Windows、macOS、Linux 的 Python 3.10 和 3.14 运行相同检查；不在 CI 请求彩票开奖网站。

## 提交要求

- 规则变更提供官方来源、生效期号及边界测试，不覆盖旧规则版本。
- 数据适配变更增加结构异常、分页异常或重复数据的回归测试。测试夹具不得带入个人日志、Cookie 或访问令牌。
- 回测变更需证明未来数据修改不影响此前生成结果，不使用完整历史提前拟合参数。
- 保持 Skill 可独立复制安装；指令和参考文件使用相对链接，脚本正确处理空格和非 ASCII 路径。
- Git 提交身份使用公开身份及 noreply 邮箱。暂存前检查 git diff，禁止提交 artifacts/、数据库、环境文件和私人目录。
- 新增依赖需说明必要性；目前运行时仅使用 Python 标准库。GitHub Actions 使用只读权限并固定 action 提交版本。

提交的问题应包括命令（移除个人路径）、预期/实际行为、退出码和复现步骤。凭据或个人信息问题请使用仓库的私密漏洞报告入口（若已启用），不要在公开 Issue 中粘贴敏感内容。
